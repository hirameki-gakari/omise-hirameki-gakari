# ひらめき係 ブランドシステム(簡易版)

「ごはんのひらめき係」と「お店のひらめき係」を姉妹サービスとして揃えるための、共通ルール。詳細な検討経緯は Claude Code のArtifactとして別途まとめてあります。ここでは実装時に参照する要点だけを残します。

## ブランド構造

- 親ブランド: **ひらめき係**(今日、どうする？を代わりに決めてくれる係)
- 子サービス: **ごはんのひらめき係**(🏠家で食べる) / **お店のひらめき係**(🍽外で食べる)
- 将来、この2つをまたぐ「今日、どうする？」ハブが3つ目のノードになり得る(3つ目のサービスができるまでは着手しない)

## 共有トークン

- 本文フォント: `Zen Kaku Gothic New`(400/500/700/900)
- 情緒的なコピー用フォント: `Shippori Mincho`(500/600)
- 背景(生成り紙色): `#F3EDE1`〜`#F7F3ED` の範囲
- 文字色(墨色): `#2A2520`〜`#2A241D` の範囲
- 角丸スケール(次回更新時の目標): `10px`(小要素) / `14px`(標準カード) / `20px`(主要カード) / `999px`(チップ・主要ボタン)

## アクセントカラー方針

各サービスのアクセントはピクセル単位で統一しない。**「温かみのあるテラコッタ〜柿色」の色域**から選ぶ。

- ごはんのひらめき係: `#B5643C`(淡: `#D99468`)
- お店のひらめき係: `#BD5A24`(淡: `#E58A47`)

新しいアクセントを追加する場合もこの色域を外れない(原色・寒色系は避ける)。

## サービスごとの個性

| | ごはん | お店 |
|---|---|---|
| 主役の画面 | 実写真(料理写真) | ブランドプレート(ジャンル別グラフィック) |
| 結果画面の地色 | 生成り紙色(昼) | 藍色(夜) |

## 共有コンポーネント

- **チップ**: ピル型・枠線1px・選択時は塗りつぶし
- **ボタン**: btn-primary(塗り・アクセント色)/ btn-secondary(枠線)/ btn-link(下線テキスト)
- **カード**: 白背景・1pxの薄い枠線・柔らかい影。情報の主役は1つだけ

## ブランドマーク(スパークアイコン)

線画のSVG(絵文字は使わない)。色は各サービスのアクセントをそのまま使う。使用箇所はフッターと将来のハブ画面のみ、ヘッダーの主役は常にサービス名のテキスト。

```html
<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v3M12 18v3M4.5 12h3M16.5 12h3M6.5 6.5l2 2M15.5 15.5l2 2M17.5 6.5l-2 2M8.5 15.5l-2 2"/><circle cx="12" cy="12" r="3.2"/></svg>
```

## キャラクター(ひらり)

迷っている人の隣でそっと今日の選択を手伝う案内役。生成り色の体・墨色の輪郭線・テラコッタのきらめき、お茶碗の絵が入ったエプロン姿。2026-09に絵柄を刷新(旧・肩掛けバッグ姿から変更)。

- アセット(背景透過・240×244・両サービス共通、顔や体形を描き直さない):
  - `assets/character/hirameki-reveal.webp` — 指差し+きらめき(通常・提案時に使用)
  - `assets/character/hirameki-thinking.webp` — 考え中(「?」付き)
  - `assets/character/hirameki-decided.webp` — 両手を挙げて喜ぶ
- 共通CSSクラス: `.mascot`(基本形、height:autoで縦横比維持) / `.mascot-intro`(導入部、幅64〜96px目安) / `.mascot-inline`(文章に添える用、幅32〜48px目安)
- 配置の考え方: 案内・結果・条件変更などの文章に寄り添わせる。カードごとの繰り返し・常時浮くキャラクター・操作を遮る吹き出しは避ける。1画面に何度も出さない
- 待ち時間の演出に使う場合、表示のために人工的な待ち時間を作らない(既存の処理・アニメーションがある場合のみ添える)
- 装飾目的の画像なので基本 `alt=""`(隣接するテキストが同じ内容を伝えている場合)
- 新しいポーズ画像を追加する場合は、必ず既存の透過PNG(上記3点、または旧バッグ姿の`ひらめきキャラクター/`フォルダ内の元画像)を参照素材として直接渡すこと。文章の説明だけで再生成すると別デザインになりやすい(2026-09に実際に発生)

## 相互導線

両サイトのフッターに、スパークマーク+1行の相互リンクを設置済み。文言は「家で食べる日は/外で食べる日は」で統一。

## トーン&マナー

- 動詞は「ひらめく」で統一。「検索する」「探す」は使わない
- 結果は言い切る(「〜かもしれません」ではなく「今日はここ」)
- スコアや属性名をユーザーに見せない。人の言葉に翻訳して出す
- 煽らない。「今すぐ!」のような広告的な語尾は使わない
